name: PR Semantic Diff

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  pull-requests: write
  contents: read

jobs:
  semantic-diff:
    uses: ./.github/workflows/reusable-semantic-diff.yml
